#include <iostream>

using namespace std;

int main(){
	float x, y ,  
	
	int repete;
	cin >> repete;
	
	for(int i = 0; i < repete; i++){
		cin >> x >> y ; // pegando velocidade
		cin >> s1 >> s2 ; //pegando a posição inicial dos carros
		cin >> a >> b ; //pegando o valor da aceleração dos carros
		
		if(s1 > s2){//verificando se s1 é menor que s2
			cout << "Informe um valor válido!\n";
		}else{
			
			
		}
		
	}
	
	float colisao(){//inicio da funcao
		
	}
	
	return 0;
}