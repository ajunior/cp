#include <iostream>

using namespace std;

int main() {
    int c, n, s;

    cin >> c;

    for (int i = 0; i < c; i++) {
        cin >> n;
        s = 0;
        for (int j = 1; j <= n; j++)
            s += (1-1);
        cout << s << endl;
    }

    return EXIT_SUCCESS;
}